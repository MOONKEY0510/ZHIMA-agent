# 隐私说明

芝麻（Zhima / ChatFloat）的默认设计是：**对话数据留在本机，只有你配置的模型服务商、搜索接口和你自己的 MCP 服务器会收到必要内容。**

## 数据存在哪里

| 内容 | 位置 |
|---|---|
| 会话、消息（含图片）、长期记忆、知识库、技能、助手、生成图片、运行统计 | 本地 SQLite：`%APPDATA%\com.chatfloat.desktop\chatfloat.db` |
| 服务商配置、联网搜索配置、MCP 服务器配置（**不含**密钥） | `%APPDATA%\com.chatfloat.desktop\providers.json` |
| 界面偏好（主题、快捷键、头像、图表风格等） | `%APPDATA%\com.chatfloat.desktop\settings.json` |
| API Key（模型服务商与联网搜索） | Windows 凭据管理器，服务名 `ChatFloat`，条目标识 `provider:<服务商ID>` / `websearch:<引擎>` |

这些文件不会离开你的电脑，除非你主动导出备份。

## 什么会发送出去

| 场景 | 发送内容 | 目标 |
|---|---|---|
| 提问 | 你的问题、该会话的上下文、会话/助手系统提示词、命中时注入的知识库片段与长期记忆、激活的技能说明、附件正文、图片 | 你选择的模型服务商 |
| 联网搜索 | 搜索关键词 | 你选择的搜索引擎（DuckDuckGo / Tavily / 博查 / SearXNG） |
| 网页抓取 | 目标 URL | 该网站 |
| MCP 工具 | 工具参数 | 本机 MCP 服务器进程（其行为取决于该服务器实现） |
| 检查更新 | 无个人数据 | `https://zhima778.cloud/chatfloat/latest.json` |

**额外的外发确认：**当本次会话中已经出现本地敏感内容（剪贴板、本地文件、PDF、截图、知识库或记忆注入、MCP 返回内容）时，把内容发往模型服务商或网络之前会再确认一次。该确认不能被此前的「本次会话允许」或「永久允许」跳过。

## 日志

- 应用**不**记录对话原文。早期版本写入的 `chat-debug.log` 会在启动时自动删除。
- 运行统计只包含：状态、错误码、耗时、工具调用次数、重试次数、模型名；不含提示词、回答正文与工具结果。
- 历史记录可整体关闭（设置 → 通用 → 会话历史）；关闭后新的对话只保留在内存中，重启即丢失。
- 工具调用的参数与结果**不写入数据库**，历史里只保留调用时间线（名称、状态、耗时）。

## 数据删除

- 删除单个会话：会话列表 → 删除；其中的消息与图片一并从数据库删除。
- 清空全部历史：设置 → 通用 → 清空会话历史。
- 完全清除：退出应用后删除 `%APPDATA%\com.chatfloat.desktop\` 目录，并在「凭据管理器 → Windows 凭据」中删除 `ChatFloat` 相关条目。

## 我们不做什么

- 不收集遥测、崩溃上报或使用统计到任何服务器。
- 不上传会话历史、记忆、知识库或生成图片。
- 备份文件**不含 API Key**：换机后需要重新填写。
- 备份是明文 JSON（含会话正文与图片），请自行决定存放位置。

## 第三方

模型服务商、搜索引擎与 MCP 服务器都由你自行配置，各自的数据处理方式遵循其自身条款。
应用自身的网络请求支持 HTTP/SOCKS5 代理配置。
